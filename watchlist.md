# Watchlist — unresolved items rolled forward between editions
# Format: - [ ] <item> — flagged <date>, why it matters, what would resolve it

(seed file — the daily run appends and resolves items here)
